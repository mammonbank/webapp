'use strict';

var shelljs = require('shelljs'),
    config = require('../core/config');

shelljs.echo('Starting bem server on port ' + config.server.bankPort);
shelljs.exec('bem server -p ' + config.server.bankPort);
